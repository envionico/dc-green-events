{
  "name": "dc-green-events",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-events.mjs",
    "build:ics": "node scripts/build-ics.mjs",
    "prebuild": "npm run validate && npm run build:ics && npm run build:seo",
    "build:seo": "node scripts/build-seo.mjs",
    "check:sources": "node scripts/check-sources.mjs",
    "import:ics": "node scripts/import-ics.mjs"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "vite": "^8.1.1"
  }
}